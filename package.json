{
  "name": "squoosh-lite",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun --filter '@squoosh-lite/core' build",
    "example": "cd apps/example && bun run dev"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.7.3"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
